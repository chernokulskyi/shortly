import { Button } from '../Button/Button';

import classes from './BoostLinks.module.scss';

export const BoostLinks = () => (
  <section className={classes.BoostLinks}>
    <h2>Boost your links today</h2>
    <Button>Get Started</Button>
  </section>
);
